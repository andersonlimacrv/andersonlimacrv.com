---
import ThemeToggle from '../ui/ThemeToggle.astro';
import LanguageSwitcher from '../ui/LanguageSwitcher.astro';
import { TYPE } from '../ui/typography';
import { defaultLocale, ui, type Locale } from '../../i18n/ui';

interface Props {
  locale?: Locale;
}

const locale = Astro.props.locale ?? defaultLocale;
const t = ui[locale];
const baseUrl = locale === defaultLocale ? '' : `/${locale}`;
const pathname = Astro.url.pathname;

const homeHref = `${baseUrl}/`;
const logoHref = locale === defaultLocale ? '/' : homeHref;

const navLinks = [
  { href: `${baseUrl}/#sobre`, label: t.navAbout },
  { href: `${baseUrl}/#projetos`, label: t.navProjects },
  { href: `${baseUrl}/#blog`, label: t.navBlog },
  { href: `${baseUrl}/#contato`, label: t.navContact },
];
---

<header class="site-header" data-astro-transition-persist="header">
  <div class="site-toolbar">
    <a
      href={logoHref}
      aria-label={t.logoLabel}
      class={`cursor-target transition-micro flex min-h-11 shrink-0 items-center px-2 $${TYPE.logo} text-foreground hover:text-muted-foreground mx-auto`}
    >AC</a>

    <nav class="site-nav" aria-label={t.navLabel}>
      {navLinks.map((link) => (
        <a
          href={link.href}
          class={`cursor-target transition-micro flex min-h-11 w-24 items-center justify-center whitespace-nowrap ${TYPE.navLink} ${
            pathname === homeHref
              ? 'text-foreground hover:text-muted-foreground'
              : 'text-muted-foreground hover:text-foreground'
          }`}
        >
          {link.label}
        </a>
      ))}
    </nav>

    <div class="site-actions">
      <ThemeToggle locale={locale} />
      <LanguageSwitcher locale={locale} />
      <button
        type="button"
        class="site-menu-toggle"
        aria-expanded="false"
        aria-controls="site-menu"
        aria-label={t.menuLabel}
        data-label-open={t.menuLabel}
        data-label-close={t.menuCloseLabel}
        title={t.menuName}
      >
        <svg
          class="site-menu-icon"
          width="20"
          height="20"
          viewBox="0 0 24 24"
          fill="none"
          aria-hidden="true"
        >
          <path
            class="site-menu-bar site-menu-bar-top"
            d="M4 7h16"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
          />
          <path
            class="site-menu-bar site-menu-bar-bottom"
            d="M4 17h16"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
          />
        </svg>
      </button>
    </div>

    <div id="site-menu" class="site-menu" aria-label={t.navLabel}>
      {navLinks.map((link) => (
        <a
          href={link.href}
          class={`cursor-target transition-micro flex min-h-11 items-center whitespace-nowrap ${TYPE.navLink} ${
            pathname === homeHref
              ? 'text-foreground hover:text-muted-foreground'
              : 'text-muted-foreground hover:text-foreground'
          }`}
        >
          {link.label}
        </a>
      ))}
    </div>
  </div>

  <script>
    import '../ui/site-header';
  </script>
</header>
</content>
