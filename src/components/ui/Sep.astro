---
import { TYPE } from './typography';

interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class:list={['select-none', TYPE.sep, 'text-muted-foreground/60', className]} aria-hidden="true">·</span>
