---
import Reveal from '../ui/Reveal.astro';
import SectionHeading from '../ui/SectionHeading.astro';
import PostCard from '../ui/PostCard.astro';
import { getPublishedPosts } from '../../lib/posts';
import { defaultLocale, ui, type Locale } from '../../i18n/ui';

interface Props {
  locale?: Locale;
}

const locale = Astro.props.locale ?? defaultLocale;
const t = ui[locale];
const posts = await getPublishedPosts(locale);
const baseUrl = locale === defaultLocale ? '' : `/${locale}`;
---

<SectionHeading
  number={t.sections.blog.number}
  title={t.sections.blog.title}
  id="blog"
  eyebrow={t.sections.blog.eyebrow}
>
  <Reveal>
    {posts.length > 0 ? (
      <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {posts.slice(0, 3).map((post) => (
          <PostCard post={post} locale={locale} />
        ))}
      </div>
    ) : (
      <p class="text-muted-foreground">
        {t.blogComingSoon}
      </p>
    )}
    <p class="mt-8">
      <a
        href={`${baseUrl}/blog`}
        class="cursor-target transition-micro inline-flex min-h-11 items-center gap-2 font-mono text-sm uppercase tracking-[0.16em] text-foreground hover:text-primary"
      >
        {t.viewAllPosts}
      </a>
    </p>
  </Reveal>
</SectionHeading>